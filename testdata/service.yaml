apiVersion: v1
kind: Service
metadata:
  name: {{ .envs.app_name }}
  namespace: {{ .namespace }}
  labels:
    app: {{ .envs.app_name }}
spec:
  ports:
    - nodePort: 30000
      port: 80
  selector:
    app: {{ .envs.app_name }}
  type: NodePort
