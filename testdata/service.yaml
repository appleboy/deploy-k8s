apiVersion: v1
kind: Service
metadata:
  name: {{ .app_name }}
  namespace: {{ .namespace }}
  labels:
    app: {{ .app_name }}
spec:
  ports:
    - nodePort: 30000
      port: 80
  selector:
    app: {{ .app_name }}
  type: NodePort
